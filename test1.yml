---
- name: check server
  hosts: all
  gather_facts: true
  tasks:
   - name: show gather gather_facts
     debug:
       msg: "OS Family is {{ ansible_os_family }} and Distribution is {{ ansible_distribution }}"